name: Release Module

on:
  release:
    types: [published]

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Extract Tag Version
        id: get_version
        run: echo "VERSION=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

      - name: Update module.json download URL & version
        run: |
          VERSION="${{ steps.get_version.outputs.VERSION }}"
          VER_NUM="${VERSION#v}"
          jq --arg url "https://github.com/${{ github.repository }}/releases/download/${VERSION}/module.zip" \
             --arg ver "$VER_NUM" \
             '.download = $url | .version = $ver' module.json > tmp.json && mv tmp.json module.json

      - name: Create Module Archive
        run: |
          files_to_zip=""
          for f in module.json main.js module lang languages styles scripts templates assets packs README.md readme.md LICENSE CHANGELOG.md changelog.md; do
            if [ -e "$f" ]; then
              files_to_zip="$files_to_zip $f"
            fi
          done
          zip -r module.zip $files_to_zip

      - name: Upload Release Assets
        uses: softprops/action-gh-release@v2
        with:
          files: |
            module.zip
            module.json
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
